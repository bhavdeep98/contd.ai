apiVersion: v1
kind: Service
metadata:
  name: {{ include "contd.fullname" . }}
  labels:
    {{- include "contd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "contd.selectorLabels" . | nindent 4 }}
